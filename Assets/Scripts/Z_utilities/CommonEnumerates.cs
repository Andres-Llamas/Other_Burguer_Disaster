using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace CommonEnumerates
{        
    public enum VarType { global, local };
    public enum foodType { lowerBread, upperBread, patty, lettuceSlice, tomatoSlice, onionSlice, plate, beacon};

}